import type { FormPropertyItem } from '../types'

export const defaultFormProperty: FormPropertyItem = {
  display: true,
  disabled: false,
  detail: false,
  readonly: false,
  required: false,
  validate: false,
}
